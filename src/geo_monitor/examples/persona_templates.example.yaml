schema_version: persona-template-registry-v1
registry_id: default_zh_cn
registry_version: v1
personas:
  budget_sensitive:
    template_id: budget_limit
    template: "预算有限的情况下，{seed_query}"
  quality_oriented:
    template_id: quality_focus
    template: "更看重品质和长期使用体验，{seed_query}"
  comparison_shopper:
    template_id: compare_options
    template: "{seed_query}，请对比几个主流选择"
  beginner:
    template_id: beginner_help
    template: "我是新手，{seed_query}"
  convenience_first:
    template_id: convenience
    template: "希望省心省事，{seed_query}"
fallback:
  template_id: default
  template: "{seed_query}"
